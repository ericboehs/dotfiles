" Gets the current version of a gem for a Gemfile
let @v="f\"vi\"yA, \"~> :r !gem sea -r \"^\"$\"|tail -1|cut -f2 -d' '|tr -d '()'kJA\"0j"

" Aligns a trailing comment with the previous line (used in Gemfile)
let @a="kf';;mm0jf';;i                                       `mjdt'0"

#! /usr/bin/osascript

# Update default settings (for new windows/tabs)
tell application "Terminal"
    set default settings to settings set "Solarized Dark"
end tell

# Update settings for exsting windows/tabs
tell application "Terminal"
    set current settings of first window to settings set "Solarized Dark"
end tell
